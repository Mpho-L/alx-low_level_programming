#include <stdio.h>

/**
 * main - Entry point
 * This C code defines a main() function that declares
 * a character array string and initializes it with the value
 * "\"Programming is like building a multilingual puzzle".
 * It then calls the puts() function with string as its argument,
 * which outputs the string to the console.
 * Return: Always 0 (Success)
*/

int main(void)
{
char string[] = "\"Programming is like building a multilingual puzzle";
puts(string);

return (0);
}
