#include <stdio.h>

/**
 * main - entry point to the program
 * the program prints the name of the current source file
 *
 * Return: always 0 for success
*/

int main(void)
{
printf("%s\n", __FILE__);
return (0);
}
